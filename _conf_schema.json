{
  "api_key": {
    "description": "MiniMax API Key（与 TTS 同一账户即可）",
    "type": "string",
    "default": ""
  },
  "api_base": {
    "description": "MiniMax API 地址（与 TTS 同站，默认 api.minimax.chat）",
    "type": "string",
    "default": "https://api.minimax.chat"
  },
  "model": {
    "description": "翻唱模型：music-cover-free（限免，RPM=3，音质一般）或 music-cover（付费，RPM=120，音质更好）",
    "type": "string",
    "default": "music-cover-free",
    "options": ["music-cover-free", "music-cover"]
  },
  "default_prompt": {
    "description": "默认翻唱风格描述（10~300 字符）",
    "type": "string",
    "default": "原曲风格翻唱，深情演绎"
  },
  "cooldown_seconds": {
    "description": "每次翻唱后的冷却秒数（防止刷爆限免额度）",
    "type": "int",
    "default": 300
  },
  "waiting_msg": {
    "description": "开始翻唱时的提示语",
    "type": "text",
    "default": "🎵 收到！正在翻唱中，预计 1~3 分钟，唱好了我发语音~"
  },
  "success_msg": {
    "description": "翻唱成功提示语（可空）",
    "type": "text",
    "default": "🎶 翻唱完成，请听~"
  },
  "fail_msg": {
    "description": "翻唱失败提示语（{err} 为错误信息）",
    "type": "text",
    "default": "翻唱失败：{err}"
  }
}